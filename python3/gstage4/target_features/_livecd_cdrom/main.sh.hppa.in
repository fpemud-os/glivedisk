#!/bin/bash

die() {
    echo "$1"
    exit 1
}

SRC_DIR=$(dirname $(realpath $0))/rootfs

mkisofs -R -l -J -V "%VOL_ID%" -o "%FILEPATH%" "${SRC_DIR}"/ || die "Cannot make ISO image"
# pushd "${clst_target_path}/"
# palo -f boot/palo.conf -C "${1}"
# popd
